# 🚨 CRITICAL BUG - YOUTUBE CHANNEL AUTO DETECTION IS STILL BROKEN

STOP implementing new features.

Fix this issue FIRST.

The endpoint:

`/api/connected/auto-detect-youtube`

is still failing and returning:

```json
{
  "error": "youtube_scope_required",
  "message": "YouTube access not authorized. Please re-authorize with Google and grant YouTube permissions when prompted."
}
```

THIS IS NOT ACCEPTABLE.

Users who have already signed in with Google should NOT be forced into another broken authorization flow.

---

# REQUIRED FIX

Investigate the entire Google authentication flow.

Verify:

* access token retrieval
* refresh token handling
* OAuth scopes
* Clerk external account integration
* Google provider configuration
* backend token extraction
* YouTube API client initialization

---

# ABSOLUTE REQUIREMENT

The endpoint:

```http
GET /api/connected/auto-detect-youtube
```

MUST successfully:

1. Retrieve the authenticated Google access token.
2. Initialize the YouTube API client.
3. Call:

```js
youtube.channels.list({
  mine: true,
  part: "snippet,statistics"
})
```

4. Return:

```json
{
  "channelId": "...",
  "channelTitle": "...",
  "avatar": "...",
  "subscriberCount": ...
}
```

WITHOUT requiring another Google login.

---

# DO NOT RETURN

```json
{
  "error": "youtube_scope_required"
}
```

or

```json
{
  "error": "google_auth_required"
}
```

or

```json
{
  "error": "failed_to_connect"
}
```

These are symptoms of a broken implementation.

---

# FIND THE ROOT CAUSE

Do NOT add temporary workarounds.

Do NOT hide the problem.

Do NOT tell the user to "reauthorize".

Fix:

* missing scopes
* token retrieval
* OAuth callback
* Clerk integration
* Google provider configuration
* backend session handling

until the endpoint works correctly.

---

# SUCCESS CRITERIA

After a user signs in with Google:

* no second OAuth flow should be required.
* no "Connect Google Again" step should exist.
* `/api/connected/auto-detect-youtube` must work immediately.
* the user's YouTube channel should be detected automatically.
* the dashboard should show the connected channel without any manual action.

THIS BUG HAS HIGHER PRIORITY THAN ALL OTHER FEATURES.

DO NOT CONTINUE DEVELOPMENT UNTIL THIS ENDPOINT IS FULLY WORKING.
