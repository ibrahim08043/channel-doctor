# 🚨 CRITICAL ISSUE - YOUTUBE CONNECTION FLOW IS STILL INCOMPLETE

The endpoint:

```http
GET /api/connected/auto-detect-youtube
```

now returns:

```json
{
  "error": "youtube_oauth_required",
  "message": "Connect your YouTube channel to continue."
}
```

This means the old Clerk token problem is gone.

However, the new architecture is STILL incomplete.

---

# CURRENT PROBLEM

Whether I:

* sign in with Google
* sign in with email/password

I always end up with:

```json
{
  "error": "youtube_oauth_required"
}
```

There is no successful path that actually connects the channel.

The system is stuck in a loop.

---

# REQUIRED BEHAVIOR

## Google Sign-In Users

Google Login

↓

Dashboard

↓

Auto-detect YouTube channel OR automatically redirect to:

"Connect YouTube via Google"

↓

Grant permission

↓

Refresh token saved

↓

Channel saved in DB

↓

`/api/connected/auto-detect-youtube`

returns:

```json
{
  "channelId": "...",
  "channelTitle": "...",
  "avatar": "...",
  "subscriberCount": ...
}
```

NOT:

```json
{
  "error": "youtube_oauth_required"
}
```

---

## Email Users

Email Login

↓

Connect Page

↓

Connect YouTube via Google

↓

Google consent screen

↓

Authorization code exchange

↓

Refresh token saved

↓

Channel saved in database

↓

Auto-detect succeeds

---

# TRACE THE ENTIRE FLOW

I want proof of each step:

### Step 1

POST:

```http
/api/youtube-oauth/start
```

Must return a valid Google authorization URL.

---

### Step 2

After consent,

Google redirects back correctly.

Verify:

* callback URL
* query parameters
* authorization code

---

### Step 3

POST:

```http
/api/youtube-oauth/exchange
```

Must:

* exchange the code
* receive access token
* receive refresh token

Show proof.

---

### Step 4

Verify the database.

Confirm:

```text
youtube_refresh_token
youtube_token_expiry
```

are actually populated.

---

### Step 5

Using the stored refresh token,

call:

```js
youtube.channels.list({
  mine: true,
  part: "snippet,statistics"
})
```

and verify the response.

---

# DO NOT STOP AT:

```json
{
  "error": "youtube_oauth_required"
}
```

That response should only appear BEFORE the connection is established.

After the user clicks "Connect YouTube via Google" and grants permission, this error must disappear permanently.

---

# MOST IMPORTANT

I want to see where the chain breaks:

1. OAuth URL generation
2. Google callback
3. Authorization code exchange
4. Access token
5. Refresh token
6. Database save
7. Channel detection

Find the exact failing step and fix it.

Do not continue implementing features until YouTube connection works completely end-to-end.
